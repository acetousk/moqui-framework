<addons default-repository="github">
    <!-- Where to get runtime directory if not present -->
    <runtime name="moqui-runtime" group="acetousk" version="3.0.0" branch="dev"/>

    <!-- Mantle Business Artifact and Integration Components -->
    <component name="mantle-udm" group="acetousk" version="2.2.0" branch="dev"/>
    <component name="mantle-usl" group="acetousk" version="2.2.0" branch="dev"/>

    <!-- Moqui Applications -->
    <component name="SimpleScreens" group="acetousk" version="2.2.0" branch="dev"/>
    <component name="PopCommerce" group="acetousk" version="2.2.0" branch="dev"/>

    <!-- Moqui Tool Components -->
<!--    <component name="moqui-hazelcast" group="acetousk" version="1.1.3" branch="dev"/>-->

    <!-- Moqui Applications -->
    <component name="PopRestStore" group="acetousk" version="1.1.0" branch="vuestorefront"/>

    <!-- Third Party Components -->
    <component name="Sales" group="xolvegroup" version="" branch="main"/>
    
    <!-- Component Sets -->
    <component-set name="custom" components="PopCommerce,HiveMind,MarbleERP,PopRestStore"/>
    <component-set name="vue-storefront" components="PopCommerce,MarbleERP,PopRestStore"/>

    <!-- Release builds:
        gradle getComponentSet -PcomponentSet=custom -PlocationType=release
        - these make the source distro for each
        - to build the demo war: gradle load test addRuntime
    -->
</addons>
